I"q<h3 id="call">Call</h3>

<p>一句话介绍<code>call</code>:</p>

<p><code>call() 方法在使用一个指定的 this 值和若干个指定的参数值的前提下调用某个函数或方法。</code></p>

<p>例如:</p>

<pre><code>var foo = {
    value: 1
};

function bar() {
    console.log(this.value);
}

bar.call(foo); // 1
</code></pre>

<p>注意两点:</p>

:ET